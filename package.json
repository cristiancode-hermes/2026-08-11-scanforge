{
  "name": "scanforge",
  "version": "1.0.0",
  "private": true,
  "description": "Scanforge — generador de códigos QR con analítica de escaneos",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:api": "npm run build -w apps/api",
    "build:web": "npm run build -w apps/web",
    "build": "npm run build:api && npm run build:web",
    "test": "npm test -w apps/api"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
