# Scanforge API — Environment
# Local dev (SQLite)
DATABASE_TYPE=better-sqlite3
DATABASE_PATH=data/scanforge.db
JWT_SECRET=scanforge-dev-secret-change-me
SEED_DB=true
PORT=3045

# Producción (Neon PostgreSQL) — descomentar en deploy
# DATABASE_TYPE=postgres
# DATABASE_URL=postgres://user:pass@host/db
